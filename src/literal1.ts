import { LOCALE, localeOMG } from "./locale";

const translate = (lang: localeOMG, text: string): string => {
  // translate text

  return text;
};

translate(LOCALE.EN, 'text');
translate(LOCALE.RU, 'text');
